approval-finalize-admin-1787246530545-2ea6fv26fb6
approval-finalize-client-1787246506126-iplzs2e22n
approval-finalize-client-1787246530545-2ea6fv26fb6
```

### Requirements

These are test records generated during development/testing and I want them cleaned up.

Before deleting anything:

1. Inspect the PostgreSQL schema and foreign-key relationships.
2. Find EVERY foreign key that directly or indirectly prevents these target users from being deleted.
3. Determine the correct child-to-parent deletion order.
4. Do not guess table or column names. Inspect the actual schema.
5. Do not disable foreign-key constraints.
6. Do not drop any constraints, tables, columns, indexes, or database objects.
7. Do not use `TRUNCATE`.
8. Do not delete unrelated production records.

We have previously encountered relationships including, but not necessarily limited to:

```text
profiles.user_id -> users.id
jobs.client_id -> users.id
candidates.user_id -> users.id
messages.sender_id -> users.id
client_profiles.user_id -> users.id
notifications.user_id -> users.id
job_submissions.client_id -> users.id
job_submissions.job_id -> jobs.id
message_threads.job_id -> jobs.id
organizations.created_by -> users.id
```

Do NOT assume this list is complete. Query the current schema to find all foreign-key dependencies.

### Important handling of indirect dependencies

Some records may not reference `users.id` directly.

For example:

```text
users
  -> jobs
      -> job_submissions
      -> message_threads
```

Those grandchildren/descendant records must be handled before deleting the parent job.

Recursively inspect dependencies so that the cleanup does not repeatedly fail with foreign-key violations.

### Organizations

Be especially careful with:

```text
organizations.created_by -> users.id
```

Do NOT blindly delete an organization merely because one of these test users is its `created_by`.

First determine whether the organization itself is test data associated exclusively with these test records.

If deleting an organization could affect legitimate/non-target users or production data, preserve the organization and safely reassign/clear `created_by` only if the schema permits and there is an appropriate valid value.

Do not delete legitimate organization data.

### Execution safety

Perform the cleanup inside a PostgreSQL transaction.

Before the DELETE operations, show me:

* how many of the supplied IDs currently exist in `"users"`
* which dependent tables contain matching records
* the number of rows that will be affected in each table
* the proposed deletion/update order

Then execute the cleanup only for records belonging to or directly associated with these target test users.

Use exact ID matching. Do not use broad matching such as:

```sql
LIKE '%test%'
```

or email/name matching.

If any dependency is ambiguous and deleting it could affect non-target users, STOP and tell me what you found instead of deleting it.

After cleanup, verify with:

```sql
SELECT id
FROM "users"
WHERE id IN (...all target IDs...);
```

The expected result is zero rows for target IDs that existed before cleanup.

Also verify that no orphaned foreign-key references remain.

Finally, report:

* number of users deleted
* number of dependent records deleted per table
* number of records updated/reassigned, if any
* supplied IDs that did not exist
* anything intentionally preserved and why

Do not modify application source code or database schema for this task. This should be a targeted database data-cleanup operation only.
