In `client/src/pages/Home.tsx`, find the stats banner that was recently added
below the hero section. It currently renders but doesn't match the intended
design. Apply ALL of the following fixes:

---

## FIX 1 — Stat value size
Change the stat value text size from text-4xl to text-5xl (or larger).
The numbers should look visually dominant — bold and large.

  Before: className="... text-4xl ..."
  After:  className="... text-5xl ..."

---

## FIX 2 — Cell alignment
Each stat cell is currently centered. Change to LEFT-ALIGNED.

  Before: items-center justify-center (or text-center)
  After:  items-start

---

## FIX 3 — Cell padding
Increase horizontal padding so each cell has breathing room from the dividers.

  Before: px-6
  After:  px-8

---

## FIX 4 — Stats grid background
The stats grid should appear slightly darker than the hero above it, creating
a visible section separation. Wrap the grid in a div with:

  className="w-full bg-black/20"

This gives the impression of a distinct band at the bottom of the hero.

---

## FIX 5 — Top border on the stats grid
Ensure there is a full-width 1px divider line directly above the stats grid:

  <div className="w-full border-t border-white/10" />

This line should sit between the tagline text and the stats grid, spanning
the full width of the hero (no container padding).

---

## FIX 6 — Tagline text above the divider
Replace the existing tagline above the stats (if it reads "Hundreds of clients,
thousands of talents, Fastest onboarding in the market.") with:

  "500+ clients globally · 2,000+ talents placed · Onboarding in 72 hours"

Styling:
- text-sm text-center text-white/60 mb-4
- "500+ clients" → font-semibold text-white
- "2,000+ talents placed" → font-semibold text-white  
- "72 hours" → font-bold text-white
- Dots (·) → text-white/30 mx-2

---

## FINAL EXPECTED RESULT
- Tagline row (centered, small)
- Full-width thin divider line
- 5-column stats grid with slightly darker bg, left-aligned large bold values,
  muted uppercase labels, vertical column dividers