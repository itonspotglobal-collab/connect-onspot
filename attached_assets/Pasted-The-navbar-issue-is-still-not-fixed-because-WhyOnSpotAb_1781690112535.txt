The navbar issue is still not fixed because `WhyOnSpotAbout.tsx` appears to have its own page-specific navigation/header.

Please fix the page-specific navbar inside:

client/src/pages/WhyOnSpotAbout.tsx

Important:
Do not only edit the global/shared navbar. This page has its own `<header>` inside `WhyOnSpotAbout.tsx`.

From DevTools, the problematic markup is around:

<header
  className="fixed inset-x-0 top-0 z-50 border-b border-slate-800/60 bg-slate-950/90 backdrop-blur-xl"
>
  <div
    className="mx-auto flex max-w-5xl items-center justify-between gap-4 px-6 py-4 lg:px-8"
  >

The issue is the header is `fixed inset-x-0`, so the full dark navigation bar stretches across the entire viewport. The desired result is that the page-specific navbar should look like a contained floating navbar, not a full-width strip.

Please update the page-specific header so that:
1. The full dark background does NOT span the entire viewport.
2. The navbar content is inside a centered, rounded container.
3. The navbar width matches the page design/content width.
4. The navbar should appear as a contained pill/card at the top, not an edge-to-edge bar.
5. It should still stay fixed/sticky at the top if that is intended.
6. It should remain responsive.

Suggested implementation:

Replace the current header structure with a fixed outer wrapper that has no full-width background:

<header className="fixed inset-x-0 top-4 z-50 px-4 sm:px-6 lg:px-8">
  <div className="mx-auto flex max-w-5xl items-center justify-between gap-4 rounded-2xl border border-slate-800/60 bg-slate-950/90 px-6 py-4 shadow-lg backdrop-blur-xl">
    ...
  </div>
</header>

Key changes:
- Move `border`, `bg-slate-950/90`, and `backdrop-blur-xl` from the `<header>` to the inner `<div>`.
- Remove `top-0` and use `top-4` if the design needs spacing from the top.
- Keep `inset-x-0` only on the transparent wrapper.
- Add `rounded-2xl` or existing rounded style to the inner nav container.
- Keep max width on the inner nav container.
- Ensure the hero section top padding is adjusted if needed so content does not hide behind the fixed navbar.

Also check if the page has duplicate navigation:
- If `App.tsx` or a layout already renders a global navbar, remove or disable the duplicate navbar from `WhyOnSpotAbout.tsx`.
- The page should have only one visible navigation bar.

Expected result:
- No full-width dark bar across the page.
- Only the contained navbar/card is visible.
- The navbar aligns with the OnSpot page design.
- No horizontal overflow.
- Mobile still works.

After implementing, report:
1. Whether the issue was caused by the page-specific navbar.
2. Whether there was a duplicate/global navbar.
3. Exact className before and after.
4. Files changed.