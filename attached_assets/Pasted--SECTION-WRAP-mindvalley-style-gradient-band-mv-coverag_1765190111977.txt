/* SECTION WRAP – mindvalley-style gradient band */
.mv-coverage {
  padding: 64px 24px 56px;
  background: radial-gradient(circle at 0% 0%, #e0e9ff 0, #f8f5ff 40%, #ffffff 100%);
  display: flex;
  justify-content: center;
}

.mv-coverage-inner {
  width: 100%;
  max-width: 1120px;
}

/* TOP ROW */
.mv-coverage-top {
  display: grid;
  grid-template-columns: minmax(0, 1.05fr) minmax(0, 1.2fr); /* map slightly bigger */
  gap: 40px;
  align-items: center;
  margin-bottom: 32px;
}

/* COPY */
.mv-title {
  margin: 0 0 12px;
  font-size: 48px;
  line-height: 1.05;
  letter-spacing: -0.03em;
  color: #0f172a;
}

.mv-subtitle {
  margin: 0 0 20px;
  font-size: 16px;
  line-height: 1.5;
  color: #4b5563;
}

.mv-stats-pill {
  display: inline-flex;
  gap: 16px;
  align-items: center;
  padding: 10px 18px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 18px 35px rgba(15, 23, 42, 0.08);
  border: 1px solid rgba(148, 163, 184, 0.35);
  font-size: 13px;
  color: #111827;
}

.mv-stats-pill span + span::before {
  content: "•";
  margin: 0 10px 0 2px;
  color: #9ca3af;
}

/* MAP HERO */
.mv-map-wrap {
  position: relative;
}

.mv-map-glow {
  position: absolute;
  inset: -15%;
  background:
    radial-gradient(circle at 20% 0%, rgba(129, 140, 248, 0.5), transparent 60%),
    radial-gradient(circle at 80% 80%, rgba(45, 212, 191, 0.45), transparent 60%);
  filter: blur(10px);
  opacity: 0.7;
  z-index: 0;
}

.mv-map-card {
  position: relative;
  z-index: 1;
  border-radius: 32px;
  overflow: hidden;
  padding: 28px 30px;
  background: rgba(255, 255, 255, 0.96);
  box-shadow: 0 32px 80px rgba(15, 23, 42, 0.25);
}

.mv-map-card img {
  display: block;
  width: 100%;
  height: auto;
}

/* BOTTOM REGIONS STRIP */
.mv-regions {
  margin-top: 16px;
  padding-top: 18px;
  border-top: 1px solid rgba(148, 163, 184, 0.3);
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.mv-regions-label {
  text-transform: uppercase;
  letter-spacing: 0.16em;
  font-size: 11px;
  color: #4f46e5;
}

.mv-regions-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.mv-region-pill {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 9px 14px;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.55);
  background: rgba(255, 255, 255, 0.9);
  font-size: 12px;
  cursor: pointer;
  box-shadow: 0 10px 25px rgba(15, 23, 42, 0.08);
  transition: transform 0.18s ease, box-shadow 0.18s ease, background 0.18s ease;
}

.mv-region-pill .dot {
  width: 8px;
  height: 8px;
  border-radius: 999px;
  background: #3b82f6;
}

.mv-region-pill .name {
  font-weight: 600;
  color: #111827;
}

.mv-region-pill .meta {
  color: #6b7280;
}

.mv-region-pill.is-active,
.mv-region-pill:hover {
  transform: translateY(-2px);
  background: linear-gradient(to right, #e0ecff, #f5f3ff);
  box-shadow: 0 18px 40px rgba(79, 70, 229, 0.25);
}

/* RESPONSIVE */
@media (max-width: 960px) {
  .mv-coverage {
    padding: 40px 16px 36px;
  }

  .mv-coverage-top {
    grid-template-columns: minmax(0, 1fr);
    gap: 24px;
  }

  .mv-map-card {
    border-radius: 24px;
  }

  .mv-title {
    font-size: 34px;
  }
}