Fix the Vanessa admin/training page so it stays accessible without authentication for now while the login system is still unfinished.

Current issue:
- The page /admin/vanessa-responses should remain accessible publicly for development/testing.
- Right now, the published site shows “Authentication required. Please log in as an admin.” when using Vanessa training features.
- I do NOT want to protect this page yet.
- I only want the auth requirement temporarily removed from the Vanessa training flow until login is finished.

Please make these changes:

1. In client/src/App.tsx:
   - Keep /admin/vanessa-responses publicly accessible.
   - Keep /admin/vanessa-learning publicly accessible too if it is also being used for testing.
   - Do NOT wrap them in AdminProtectedRoute yet.

2. In client/src/pages/TrainingChat.tsx:
   - Remove the production-only token requirement that throws:
     “Authentication required. Please log in as an admin.”
   - Do not require localStorage token for now.
   - Allow the training request to be sent without Authorization header.
   - Keep the request working in both development and production while login is unfinished.

3. In server/routes.ts:
   - Temporarily remove the production auth middleware from these Vanessa training endpoints:
     - POST /api/train/chat/stream
     - POST /api/train/correct
   - Also check if /api/site/reindex is tied to the same temporary middleware and decide whether it should remain public for now or stay protected. If unsure, leave a comment.
   - The result should be that Vanessa training works without authentication for now.

4. Add clear TODO comments in the code saying this is temporary and must be re-protected once the admin login/auth system is complete.

5. After making the changes, summarize:
   - which files were updated
   - what auth checks were removed temporarily
   - what TODOs were added for restoring admin protection later

Important:
- This is a temporary development-phase change only.
- Do not refactor unrelated auth code.
- Only remove auth requirements for the Vanessa admin/training flow that is blocking access right now.