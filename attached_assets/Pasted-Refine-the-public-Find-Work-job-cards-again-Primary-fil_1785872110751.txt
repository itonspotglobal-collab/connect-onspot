Refine the public Find Work job cards again.

Primary file:
- `client/src/pages/FindWorkAllJobs.tsx`

Also inspect any reusable job-card component if the card is rendered elsewhere.

VISUAL GOAL

Use the SECOND attached reference image as the visual inspiration.

Important:
- Do NOT copy it exactly
- Keep OnSpot branding
- But move away from the current large purple gradient card header
- Use a cleaner mostly-white card with subtle neutral colors
- Use soft badge colors similar in feel to the reference
- Add a compact initials-based role avatar on the left

The current result is still too visually heavy because of the large blue/purple header.

I want the card to feel:
- cleaner
- lighter
- more minimal
- more professional
- easier to scan
- more similar in visual density to the reference

1. REMOVE THE LARGE GRADIENT HEADER

The current card has a full-width blue/purple gradient header.

Replace this with a mostly white card layout.

Suggested overall style:

```tsx
<article className="
  rounded-2xl
  border border-slate-200
  bg-white
  shadow-sm
  hover:shadow-md
  transition-shadow
  overflow-hidden
">

Use only small OnSpot accent colors where appropriate instead of a large gradient block.

Dark mode should still be supported.

ADD ROLE INITIALS AVATAR

Replace the generic code icon with a compact rounded-square avatar using initials from the job title.

Examples:

Sales / Business Development Lead → SB
IT Administrator → IA
Data Migration Developer → DM
Virtual Assistant → VA
Customer Service Representative → CS

The initials should be generated dynamically from the role title.

Suggested logic:

split title into words
ignore symbols like /, -, &
use the first character of the first two meaningful words
uppercase the result
maximum 2 letters

Example helper:

function getRoleInitials(title: string) {
  const words = title
    .replace(/[^\w\s]/g, " ")
    .split(/\s+/)
    .filter(Boolean);

  if (words.length === 0) return "JB";
  if (words.length === 1) return words[0].slice(0, 2).toUpperCase();

  return `${words[0][0]}${words[1][0]}`.toUpperCase();
}

Improve the helper if needed so titles with stop words still produce useful initials.

Avatar visual:

<div className="
  flex h-11 w-11 shrink-0 items-center justify-center
  rounded-xl
  bg-indigo-100
  text-sm font-semibold
  text-indigo-700
  shadow-sm
  dark:bg-indigo-500/15
  dark:text-indigo-300
">
  {getRoleInitials(job.title)}
</div>

Do not use a random color per role unless the project already has a controlled palette.

NEW TOP ROW STRUCTURE

The top part of the card should be more like:

LEFT:

role initials avatar
job title
company name
badges

RIGHT:

salary
salary period if applicable
optional compensation badges like Commission / Equity if supported

Suggested structure:

<div className="flex flex-col gap-4 p-5 sm:flex-row sm:items-start sm:justify-between">

Left area:

<div className="flex items-start gap-3">
  {/* initials avatar */}

  <div className="min-w-0">
    <h3 className="text-base md:text-lg font-semibold text-slate-900 dark:text-white">
      {job.title}
    </h3>

    <p className="mt-0.5 text-sm text-slate-500 dark:text-slate-400">
      {job.company}
    </p>

    {/* badges */}
  </div>
</div>

Right area:

<div className="sm:text-right">
  <div className="text-base md:text-lg font-semibold text-slate-900 dark:text-white">
    {formattedSalary}
  </div>

  <div className="text-xs text-slate-500">
    {salaryPeriod}
  </div>
</div>
BADGE COLORS

Use softer, muted colors inspired by the reference.

Examples:

Urgently Hiring:

bg-amber-50 text-amber-700 border-amber-100

Remote:

bg-violet-50 text-violet-700 border-violet-100

Full Time:

bg-emerald-50 text-emerald-700 border-emerald-100

Hybrid:

bg-sky-50 text-sky-700 border-sky-100

Onsite:

bg-slate-100 text-slate-700 border-slate-200

Commission:

bg-emerald-50 text-emerald-700

Equity:

bg-fuchsia-50 text-fuchsia-700

Use:

inline-flex items-center rounded-md border px-2 py-1 text-[11px] font-medium

Do not make pills large or highly saturated.

BADGE PLACEMENT

Move the main badges directly under the company name.

Examples:

Urgently Hiring
Remote
Full Time

Use:

<div className="mt-2 flex flex-wrap gap-1.5">

Do not duplicate Remote and Full Time unnecessarily in both the badge row and metadata row unless needed for clarity.

SALARY PRESENTATION

Make salary visually strong but clean.

Examples:

₱10,000–₱15,000
per month

or:

Rate TBD

Do not use white text on a gradient header anymore.

Use dark text on the light card.

Suggested classes:

text-lg font-semibold text-slate-900 dark:text-white

Period:

text-xs text-slate-500 dark:text-slate-400
OPTIONAL ADDITIONAL COMPENSATION

If Commission and Equity fields already exist, show them under the salary as small soft badges.

Example:

<div className="mt-2 flex justify-start gap-1.5 sm:justify-end">

Do not invent these values if they do not exist.

If the fields were added in the previous implementation, reuse them.

METADATA ROW

Keep the existing details such as:

Location
Function
Contract
Posted date

But make the row lighter and more compact.

Instead of large boxed cells, use a subtle divider row.

Example:

<div className="
  grid grid-cols-2
  border-t border-slate-100
  md:grid-cols-4
  dark:border-slate-800
">

Each item:

<div className="px-5 py-3">
  <p className="text-[10px] uppercase tracking-wide text-slate-400">
    Location
  </p>

  <p className="mt-0.5 text-sm font-medium text-slate-700 dark:text-slate-200">
    Remote
  </p>
</div>

Use very subtle vertical dividers if desired.

SUMMARY

Keep the Card Preview Summary.

Do not remove it.

Keep:

2-line or 3-line clamp
smaller body text
readable width
no full description on the listing card

Suggested:

<p className="
  line-clamp-2
  text-sm
  leading-6
  text-slate-600
  dark:text-slate-300
">
SKILLS

Keep skills/tags.

Make them more compact and neutral:

rounded-full
border border-slate-200
bg-slate-50
px-2.5 py-1
text-xs
text-slate-600

Show only the first 3 or 4.

If there are more, show:

+3 more
ACTION BUTTONS

Keep:

Apply Now
View details

But make them visually cleaner.

Apply Now:

retain OnSpot blue/purple accent
smaller height
rounded-lg or rounded-full
not overly large

View details:

white/transparent background
soft border
slate text

Example:

<div className="flex flex-wrap gap-2">

Do not change existing routes or handlers.

CARD SPACING

Reduce unnecessary vertical padding.

Suggested:

top section: p-5
metadata item: py-3
summary content: px-5 py-4
actions: mt-4

Avoid making the card too tall.

DO NOT REMOVE EXISTING DATA

The card must still support:

Job title
Company
Salary
Salary period
Contract
Location
Function/category
Posted date
Card Preview Summary
Skills/tags
Urgently Hiring badge
Other existing badges
Additional compensation fields if present
Apply Now
View details
MOBILE

On mobile:

avatar + title remain aligned
salary moves below or beside title without overflow
badges wrap
metadata becomes 2 columns
action buttons remain easy to tap
no horizontal scrolling
DARK MODE

Preserve dark mode.

Use matching muted dark colors rather than reverting to a large gradient.

EXPECTED RESULT

The updated card should visually resemble the second reference in spirit:

white/minimal card
initials avatar on the left
strong job title
muted company name
compact soft badges
salary aligned right
compact metadata
summary + skills below
buttons at the bottom

But it must still feel like OnSpot and not like a direct copy of the reference.

After implementing, provide:

exact files changed
the initials-generation helper used
the badge color mapping
confirmation that the purple gradient header was removed
confirmation that all previous job details are still shown
confirmation that desktop, mobile, light mode, and dark mode were tested

The key visual change here is the shift from a “banner card” to a “content card.” That should make the initials logo and the muted badge colors feel much closer to your inspiration without losing the OnSpot look.