FIX ONLY HERO CAROUSEL SLIDES 1, 3, 4, AND 5.

Use my latest screenshots as the visual reference.
Do not redesign the carousel.
Do not change slide order, database integrations, routes, or other homepage sections.

==================================================
GLOBAL RESPONSIVE RULES FOR THESE SLIDES
==================================================

The current issue is that content scales too aggressively and overlaps on some screen sizes.

Use a shared responsive hero system:

.hero-slide {
  min-height: calc(100svh - var(--header-height));
  height: auto;
  position: relative;
}

.hero-inner {
  width: min(calc(100% - 48px), 1180px);
  margin-inline: auto;
}

For slides with two columns:
display: grid;
grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
align-items: center;
gap: clamp(36px, 6vw, 96px);

IMPORTANT:
- no fixed left/top pixel positioning for main content
- no fixed widths that cause overflow
- no transform: scale() hacks
- no absolute positioning for major text/mockup blocks
- use clamp(), min(), max-width, grid and flex
- ensure min-width: 0 on grid children
- use overflow-wrap where necessary
- preserve carousel controls in a separate bottom zone so they never overlap the slide content

At screen widths <= 1100px:
- reduce typography and visual sizes
- reduce grid gap

At <= 900px:
- stack Slides 2–5 to one column if two columns no longer fit
- text first
- visual/card second

At <= 640px:
- allow hero height to grow naturally
- do not force everything into one viewport if doing so creates overlap


==================================================
SLIDE 1 — FIX SUBHEADING
==================================================

The current subheading is too large and wraps onto two lines.

Target text:
“One system. Highest pay for talents at lower cost to companies.”

On desktop/laptop this MUST remain ONE LINE.

Use approximately:
font-size: clamp(16px, 1.35vw, 22px);
line-height: 1.4;
white-space: nowrap;

Set the subheading wrapper to:
width: max-content;
max-width: 100%;
margin-inline: auto;

Do NOT use the same responsive scale as the hero heading.

“One system.” = white / semibold
remaining text = muted lavender

At smaller widths only, allow wrapping:
@media (max-width: 700px) {
  white-space: normal;
}

Keep heading and buttons centered.


==================================================
SLIDE 3 — REBUILD PHONE TO MATCH MOCK
==================================================

The current phone is still visually different from the reference.

Use the detailed reference phone design exactly:

PHONE:
- tall narrow proportions
- desktop width approximately clamp(205px, 16vw, 235px)
- dark navy outer shell
- white inner screen
- rounded shell approx 30px+
- centered black pill/notch
- subtle dark drop shadow

TOP:
orange rounded-square icon + “OnSpot”
purple circle at far right

Title:
“Earnings summary”

Subtitle:
“Last 6 months”

EARNINGS CARD:
- pale cream background
- thin warm orange border
- rounded corners
- label:
  “TOTAL EARNED THIS MONTH”
- amount:
  “$3,455”
- pale green pill:
  “▲ 18% vs last month”

STATS:
two equal light-gray cards

AVG. RATE
$20/hr

ACTIVE
CLIENTS
2

BAR CHART:
MUST include 6 visible orange bars:
Mar Apr May Jun Jul Aug

Use visibly different heights:
Mar 35%
Apr 52%
May 38%
Jun 65%
Jul 58%
Aug 90%

The existing blank chart area is NOT acceptable.

EARNINGS BY CLIENT:
New Tech AI                         $2,000
purple progress bar almost 100%

John Roberts LLC                    $1,455
purple progress bar around 70–75%

Keep the UI dense like the reference:
- minimal wasted vertical space
- clear month labels
- tight but readable spacing
- compact internal typography

Do not alter the left hero copy.


==================================================
SLIDE 4 — TALENT NETWORK RESPONSIVE FIX
==================================================

CURRENT PROBLEM:
The heading becomes enormous, wraps badly, controls overlap, and the card does not stay balanced.

Desktop target:

LEFT:
“Thousands of
talents. Ready to
work.”

Do NOT allow single words to become isolated because the font is too large.

Use:
font-size: clamp(46px, 4.2vw, 68px);
line-height: 0.98;
letter-spacing: -0.03em;

Text column:
max-width: 500px;
min-width: 0;

Supporting heading:
font-size: clamp(20px, 1.6vw, 27px);

Body:
font-size: clamp(15px, 1vw, 17px);
max-width: 500px;

RIGHT TALENT CARD:
width: clamp(400px, 38vw, 520px);
max-width: 100%;

Do NOT position this card with fixed left/top values.

The talent card must stay entirely inside the viewport.

Its rows must dynamically size:
- avatar
- name
- role
- rating
- availability badge

Use grid/flex so long database values cannot overlap.

For each talent row:
display:grid;
grid-template-columns: auto minmax(0,1fr) auto;
gap: 12px;
align-items:center;

For rating/status area:
display:flex;
gap: 8px;
flex-wrap: wrap;
justify-content: flex-end;

Long titles:
overflow: hidden;
text-overflow: ellipsis;
white-space: nowrap;

CAROUSEL CONTROLS:
must sit BELOW the main content visually.
Do not allow the CTA or text to cover them.

At 1366×768 and similar short screens:
- reduce heading
- reduce section gaps
- reduce card height slightly
- keep all controls fully visible

At <= 900px:
stack text and talent card.

Do not shrink the text to unreadable sizes merely to retain two columns.


==================================================
SLIDE 5 — JOBS RESPONSIVE FIX
==================================================

CURRENT PROBLEM:
Heading is too large, wraps excessively, and bottom controls/CTA collide.

Use desktop heading:
font-size: clamp(46px, 4.15vw, 68px);
line-height: 0.98;
letter-spacing: -0.03em;

Target wrapping:
“Hundreds of
high-paying jobs.
Open right now.”

Do not allow:
“Hundreds of high-paying jobs.” to become 4–5 lines unless mobile.

Text column:
max-width: 510px;
min-width: 0;

Supporting headline:
font-size: clamp(20px, 1.6vw, 27px);

Body:
font-size: clamp(15px, 1vw, 17px);

JOBS CARD:
width: clamp(400px, 37vw, 500px);
max-width: 100%;

Do NOT use fixed-position coordinates.

Each live database job row must safely handle variable content.

Use:
display:grid;
grid-template-columns: minmax(0,1fr) auto;
gap: 12px;

Left job info:
min-width: 0;

Job title:
overflow-wrap: anywhere;

Badges:
display:flex;
flex-direction:column;
align-items:flex-end;
gap:6px;

If compensation is long:
allow it to wrap within the left area instead of pushing the badges outside.

Keep:
Featured / Hiring now
statuses responsive.

CAROUSEL CONTROLS:
must never overlap:
- Browse all jobs button
- text
- jobs card

Give the content enough bottom padding for controls:
padding-bottom: clamp(90px, 11vh, 120px);

At short desktop heights:
reduce internal vertical margins BEFORE reducing font size excessively.

At <= 900px:
stack text and jobs card.


==================================================
CAROUSEL CONTROL LAYOUT
==================================================

Use ONE shared control component across slides.

Position controls inside the hero-inner container.

Suggested desktop:
position:absolute;
left: max(24px, calc((100vw - 1180px) / 2));
bottom: clamp(22px, 3.5vh, 36px);

BUT ensure the main slide content has enough bottom padding that controls never cover it.

Better if possible:
use a CSS grid with:
row 1 = main content
row 2 = controls

This is preferred over overlapping absolute positioning.

==================================================
SHORT-SCREEN HEIGHT HANDLING
==================================================

Add height-aware adjustments.

Example:
@media (max-height: 800px) and (min-width: 901px) {
  reduce:
  - heading sizes modestly
  - section vertical gaps
  - button margins
  - mockup/card dimensions
}

Do NOT simply apply transform: scale(0.8).

All components should reflow naturally.


==================================================
TEST THESE EXACT VIEWPORTS
==================================================

1920×1080
1600×900
1440×900
1366×768
1280×720
1024×768
768×1024
390×844

Verify each of Slides 1, 3, 4, and 5.

Acceptance criteria:
- Slide 1 subheading is one line on desktop/laptop.
- Slide 3 phone closely matches the supplied mock including orange chart bars.
- Slide 4 has no overlapping title, CTA, talent card, or carousel controls.
- Slide 5 has no overlapping title, CTA, job card, or carousel controls.
- no horizontal overflow
- no clipped text
- no fixed-coordinate layout dependence
- database-driven content still works
- buttons/routes still work
- other slides remain unchanged