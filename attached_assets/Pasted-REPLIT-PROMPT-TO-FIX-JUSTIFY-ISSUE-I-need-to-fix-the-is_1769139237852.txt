REPLIT PROMPT TO FIX JUSTIFY ISSUE

I need to fix the issue where the blog text alignment (justify, left, center, right) works properly inside the TipTap editor but does NOT appear justified in the rendered/published blog page.

Here’s what must be corrected:

1. Ensure TipTap’s alignment attributes are not removed

Check the API or server-side sanitization:

Published post HTML currently removes style="text-align: justify" and alignment attributes.

Update sanitization rules to ALLOW:

style="text-align: …"

<p align="…">

TipTap attributes like textAlign

Make sure the backend DOES NOT strip these alignment rules.

2. Fix blog post renderer

Open the blog post display page (likely something like:
client/src/pages/InsightPost.tsx or client/src/pages/Insights/[slug].tsx).

Where the HTML is rendered, ensure:

<div
  className="prose max-w-none"
  dangerouslySetInnerHTML={{ __html: post.content }}
/>


ADD a global style to support alignment tags:

.prose p[style*="text-align"] {
  text-align: inherit !important;
}

.prose p {
  white-space: normal;
}


Or allow inline styles:

.prose p {
  text-align: var(--textAlign);
}

3. TipTap configuration must include the TextAlign extension

If not already enabled, ensure:

import TextAlign from '@tiptap/extension-text-align'

Editor({
  extensions: [
    TextAlign.configure({
      types: ['heading', 'paragraph'],
    }),
  ]
})

4. Confirm the saved content includes alignment attributes

After editing a post, verify that the JSON/HTML saved includes:

<p style="text-align: justify;">...</p>


If not, fix the editor output to preserve these attributes.

EXPECTED RESULT

When I justify text in the editor:

The editing view shows justified text (already works)

The PUBLISHED post also displays justified text (currently does NOT work)

Inline styles or TipTap attributes must be preserved and rendered

Please scan the relevant files and apply all necessary changes:

TipTap editor configuration

Backend sanitization

Blog renderer (HTML output)

CSS for handling alignment

Do NOT remove any features—only ensure alignment persists from the editor to the published blog.